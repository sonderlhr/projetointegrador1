# Exercício A - Área do Triângulo
# Entrada de dados
base = float(input("Digite o valor da base do triângulo: "))
altura = float(input("Digite o valor da altura do triângulo: "))
# Processamento
area = (base * altura) / 2
# Saída de dados
print(f"A área do triângulo é: {area:.2f}")
